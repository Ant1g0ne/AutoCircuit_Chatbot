⚡ AutoCircuit Pro | 智能车辆电路图导航助手
AutoCircuit Pro 是一个基于大语言模型（LLM）的垂直领域知识库检索系统。它专为解决非标准化搜索痛点而设计，能够通过意图识别、智能纠错、多轮状态机引导，帮助用户从海量 CSV 资料库中精准定位车辆电路图文档。

🚀 核心特性 (Key Features)
🧠 1. 深度语义理解 (LLM-Powered)
        智能意图提取：集成 GPT-4o-mini，从自然语言中提取 Brand（品牌）、Series（车系）、Part（零部件）。
        错别字纠正：自动识别并修正行业术语拼写错误（例：小忪 → 小松，2ooo → 2000）。
        同义词泛化：搜索“保险丝”可自动匹配“熔断器”、“配电盒”等专业术语。

🛡️ 2. 上下文感知与逻辑隔离
        品牌硬隔离：系统具有上下文记忆能力。当用户搜索“红岩”时，后续引导选项只会显示“杰狮”、“杰卡”，绝对屏蔽其他品牌（如解放 J6P）的干扰。
        动态选项生成：基于当前检索结果动态生成 A/B/C 选项，杜绝无效引导。
        
🔄 3. 严密的状态机引导
        剧本式交互：遵循 搜索 -> 确认车系 -> 确认类型 -> 展示结果 的严格状态流。
        贪婪匹配策略：
            1.优先尝试 全匹配 (Brand + Series + Part)。
            2.失败则降级为 车型匹配 (Brand + Series)。
            3.最后兜底为 高阈值模糊搜索 (Fuzzy Ratio > 40)，确保用户“永不空手而归”。

💻 4. Pro 级交互体验
        全功能控制台：侧边栏实时显示 API 连接状态、资料库数量、以及详细的 Debug 推理过程。
        结果持久化：历史搜索结果以卡片形式保留在对话流中，支持随时回看和下载。
        开屏引导：提供快捷搜索入口，降低冷启动门槛。

🛠️ 技术栈 (Tech Stack)
Frontend: Streamlit (Web 交互与状态管理)
LLM: OpenAI GPT-4o-mini (意图解析与实体提取)
Data Processing: Pandas (CSV 数据清洗与过滤)
Search Algorithm: FuzzyWuzzy (Levenshtein Distance 模糊匹配) + RegEx

🏃‍♂️ 快速开始 (Quick Start)
1. 克隆仓库
Bash
git clone https://github.com/YourUsername/AutoCircuit-Chatbot.git
cd AutoCircuit-Chatbot

3. 安装依赖
建议使用虚拟环境（Virtualenv）：
Bash
pip install -r requirements.txt

3. 配置密钥 (重要！)
在项目根目录下创建 .streamlit 文件夹，并新建 secrets.toml 文件：
Bash
mkdir .streamlit
#Windows用户请手动创建 .streamlit/secrets.toml

在 secrets.toml 中填入你的 OpenAI API Key：
Ini, TOML
OPENAI_API_KEY = "sk-proj-xxxxxxxxxxxxxxxxxxxxxxxx"

4. 运行应用
Bash
streamlit run app.py
浏览器将自动打开 http://localhost:8501。

📂 项目结构
Plaintext
AutoCircuit-Chatbot/
├── app.py                # 主程序入口 (UI渲染、状态机逻辑)
├── utils.py              # 核心工具库 (LLM接口、搜索算法、同义词库)
├── requirements.txt      # 依赖包列表
├── .gitignore            # Git 忽略规则
├── .streamlit/
│   └── secrets.toml      # 敏感配置文件 (不上传 GitHub)
└── data/
    └── 资料清单.csv       # 核心数据文件
    
🧠 搜索逻辑流程图
代码段
graph TD
    A[用户输入 "红岩杰狮保险丝"] --> B(LLM 解析 & 纠错)
    B -->|提取实体| C{是否提取成功?}
    C -->|是| D[贪婪匹配策略]
    C -->|否| E[模糊搜索兜底]
    
    D --> D1[尝试 Brand+Series+Part]
    D1 -->|无结果| D2[尝试 Brand+Series]
    D2 -->|无结果| D3[尝试 Brand Only]
    
    D3 --> F{结果数量 > 5 或 存在歧义?}
    E --> F
    
    F -->|是| G[状态机引导: 选择车系]
    G --> H[状态机引导: 选择类型]
    F -->|否| I[展示最终结果卡片]


Developed by Ant1g0ne

